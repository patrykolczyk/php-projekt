{# Add action template for Albums controller #}
{% extends 'base.twig' %}

{% block title %}{{  'Add category'|trans }}{% endblock %}

{% block head %}
{{ parent() }}
<style type="text/css">
    body { margin: 20px; }
</style>
{% endblock %}

{% block content %}
    <h1>
        {{ 'Add category'|trans }}
    </h1>
    <form action="{{ url('categories_add') }}" method="post">
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-default"  name="submit" value="{{ 'Save'|trans }}" />
    </form>
{% endblock %}
