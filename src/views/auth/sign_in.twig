{# Add action template for User controller #}
{% extends 'base.twig' %}

{% block title %}{{  'Add user'|trans }}{% endblock %}

{% block head %}
{{ parent() }}
<style type="text/css">
    body { margin: 20px; }
</style>
{% endblock %}

{% block content %}
    <h1>
        {{ 'Add user'|trans }}
    </h1>
    <form action="{{ url('sign_in') }}" method="post">
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-default"  name="submit" value="{{ 'Save'|trans }}" />
    </form>
{% endblock %}
